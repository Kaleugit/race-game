# PREREQUISITES — <project-name>

Itens que o humano precisa resolver antes de execução automatizada.

## Ambiente
- [ ] TBD

## Acessos e credenciais
- [ ] TBD

## Ferramentas de governança
- [ ] `git` e `bash` disponíveis (Git Bash no Windows).
- [ ] PyYAML instalado para hooks de governança (`python3 -m pip install --user pyyaml`).
- [ ] GNU coreutils/gnu-sed no PATH para `scripts/validate-all.sh` no macOS (`/opt/homebrew/opt/coreutils/libexec/gnubin`).
